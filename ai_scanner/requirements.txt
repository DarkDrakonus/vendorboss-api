Pillow>=10.0.0
imagehash>=4.3.1
numpy>=1.24.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
tqdm>=4.65.0
pillow-heif>=0.13.0  # HEIC/HEIF support for iPhone photos
opencv-python-headless>=4.8.0  # Card detection and perspective correction

# Phase 2 — CLIP embeddings (install separately, ~2GB)
# pip3 install torch torchvision
# pip3 install git+https://github.com/openai/CLIP.git
